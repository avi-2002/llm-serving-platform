apiVersion: v1
kind: Service
metadata:
  name: llm-serving
  labels:
    app.kubernetes.io/name: llm-serving
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-serving
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
