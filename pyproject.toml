[project]
name = "low-latency-llm-serving"
version = "0.1.0"
description = "A learning-first platform for measuring and serving open-source LLMs"
readme = "README.md"
authors = [
    { name = "avi-2002", email = "92429854+avi-2002@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116,<1",
    "psutil>=7,<8",
    "safetensors>=0.5,<1",
    "torch==2.10.0",
    "transformers>=4.57,<5",
    "uvicorn[standard]>=0.35,<1",
]

[project.scripts]
local-llm = "low_latency_llm_serving.cli:main"
llm-api = "low_latency_llm_serving.api.server:main"
llm-benchmark = "low_latency_llm_serving.benchmark:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "httpx2>=2.9.1",
    "pytest>=8,<10",
    "ruff>=0.12,<1",
]
